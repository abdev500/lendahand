{{- if and .Values.frontend.enabled .Values.frontend.ingress.enabled }}
{{- $fullName := include "lendahand.fullname" . }}
{{- $svcPort := .Values.frontend.service.port }}
{{- if and .Values.frontend.ingress.className (not (hasPrefix "nginx" .Values.frontend.ingress.className)) }}
{{- fail "Only nginx ingress controller is supported" }}
{{- end }}
{{- range .Values.frontend.ingress.hosts }}
{{- $host := .host | default "" }}
{{- $paths := .paths | default (list (dict "path" "/" "pathType" "Prefix")) }}
{{- $tlsEnabled := $.Values.frontend.ingress.tls | default false }}
{{- $clusterIssuer := $.Values.frontend.ingress.tlsClusterIssuer | default "letsencrypt-prod" }}
{{- if $host }}
{{- $secretName := printf "%s-tls" ($host | replace "." "-" | replace "_" "-" | lower) }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $fullName }}-frontend
  labels:
    {{- include "lendahand.frontend.labels" $ | nindent 4 }}
  annotations:
    {{- with $.Values.frontend.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if $tlsEnabled }}
    cert-manager.io/cluster-issuer: {{ $clusterIssuer | quote }}
    {{- end }}
spec:
  {{- if $.Values.frontend.ingress.className }}
  ingressClassName: {{ $.Values.frontend.ingress.className }}
  {{- end }}
  {{- if $tlsEnabled }}
  tls:
    - hosts:
        - {{ $host | quote }}
      secretName: {{ $secretName | quote }}
  {{- end }}
  rules:
    {{- range $paths }}
    - host: {{ $host | quote }}
      http:
        paths:
        - path: {{ .path | default "/" }}
          pathType: {{ .pathType | default "Prefix" }}
          backend:
            service:
              name: {{ $fullName }}-frontend
              port:
                number: {{ $svcPort }}
    {{- end }}
{{- end }}
{{- end }}
{{- end }}
